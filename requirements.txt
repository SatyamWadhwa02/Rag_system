# ============================================================
# Quantum Computing RAG System — Requirements
# ============================================================
# Core dependencies (required)
# ── No external ML libraries required for base TF-IDF mode ──
# Python standard library only for: json, re, math, pickle, pathlib, dataclasses

# Optional: dense semantic embeddings (recommended for better retrieval)
sentence-transformers>=2.2.2   # all-MiniLM-L6-v2 model (~80MB)
torch>=2.0.0                   # required by sentence-transformers

# Optional: evaluation utilities
scikit-learn>=1.3.0            # alternative cosine_similarity, metrics
numpy>=1.24.0                  # array operations
pandas>=2.0.0                  # result dataframes / CSV export

# Optional: LangChain integration (alternative pipeline)
# langchain>=0.1.0
# langchain-community>=0.0.10
# langchain-anthropic>=0.1.0
# faiss-cpu>=1.7.4             # faster vector search at scale

# Development / testing
pytest>=7.4.0
